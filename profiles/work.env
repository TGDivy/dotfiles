export DOTFILES_PROFILE=work
# Bloomberg internal PyPI — set your actual URL
export UV_INDEX_URL=https://pypi.bloomberg.com/simple
export UV_EXTRA_INDEX_URL=https://pypi.org/simple
# cmake: use bbcmake if available, fall back to cmake
export CMAKE_FORMATTER=bbcmake
